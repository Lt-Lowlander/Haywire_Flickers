This is an attempt to create a grid with continuously changing cell colors.

The rowQuant & colQuant variables dictate the size of the grid, and the d4 variable sets the timespan between function calls.
